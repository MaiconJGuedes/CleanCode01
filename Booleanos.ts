//Booleanos

const isUserOnMajority = true

/* Sempre quando for usar o Booleans como se fosse uma pergunta.

// is, does, has

*/

const disable = true
const isDisable = true

// -----------


if (isDisable) {

}

if (isUserOnMajority) {
  
}

//Booleans sempre como perguntas / semântico

loading

isLoading

isUserProfileLoading

// evitar: loading, disable, data, fetching, list

// Ler o boolans como se tiver com um 'if' 
// if loading, if is user profile loading